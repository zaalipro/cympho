# Warnings dialyzer should suppress. Add lines as:
#   {"lib/file.ex"}                       # ignore all warnings from a file
#   {"lib/file.ex", :warning_type}        # ignore a specific warning type in a file
#   {"lib/file.ex", :warning_type, line}  # ignore a specific warning at a line
#
# Keep this file empty by default; only add entries that document a known
# acceptable warning (with a comment explaining why), never to silence a real
# issue.
[]
